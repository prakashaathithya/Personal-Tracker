# dependencies
node_modules/

# npm lockfile intentionally not committed: a Windows-generated lockfile breaks
# Vercel's Linux install (missing platform binaries, npm/cli#4828). Fresh installs
# resolve the correct per-platform binaries on every machine.
package-lock.json
**/package-lock.json

# build output
dist/
web/dist/
api/dist/
.angular/

# environment
.env
.env.*
!.env.example

# vercel
.vercel/

# logs / os
*.log
.DS_Store
Thumbs.db

# source workbook (kept locally, not deployed)
*.xlsx
.vercel
.env*
